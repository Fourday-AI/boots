{
  "name": "boots",
  "version": "0.1.0",
  "description": "Boots — a companion for building and finishing AI systems with Claude Code.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "gen:skill-docs": "bun run scripts/gen-skill-docs.ts",
    "gen:skill-docs:check": "bun run scripts/gen-skill-docs.ts --dry-run",
    "test": "bash scripts/test-e2e.sh",
    "test:live": "bash scripts/test-e2e.sh --live",
    "test:install": "bash scripts/test-install.sh"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
