# macOS
.DS_Store

# Archives / bundles (not source)
*.zip

# Private runtime — boots session state (interviews, systems, archives).
# Holds summaries of private AI sessions; never commit. Mirrors the
# state/ convention in the ai-coach-v0 product repo.
/state/

# Secrets — API keys and scoped session URLs for hosted integrations
# (Composio). Configs reference these via ${VAR} expansion, never literals.
.env
.env.*

# Boots connection state (contains ids; keep out of git)
.connection.local.json
state/systems/*/.connection.local.json
# reply-guard private ntfy topic (a push credential — keep out of git)
state/systems/reply-guard/.ntfy-topic
state/systems/reply-guard/output/launchd.log

# generated external-host skill docs (bun run gen:skill-docs --host <name>)
.agents/
.codex/
node_modules/

# ── Skills allow-list (deny-by-default) ─────────────────────────────────────
# This is an OPEN-SOURCE repo. Everything under .claude/skills/ is ignored, and
# ONLY the product skills below are un-ignored. A newly created skill dir is
# invisible to git until you add an explicit `!` line for it here — so a private
# or experimental skill can never be committed by accident (not even via
# `git add -A`). When you ship a NEW product skill, add one line below.
/.claude/skills/*
!/.claude/skills/boots/
!/.claude/skills/boots-clarify/
!/.claude/skills/boots-scope/
!/.claude/skills/boots-build/
!/.claude/skills/boots-review/
!/.claude/skills/boots-verify/
!/.claude/skills/boots-ship/
!/.claude/skills/boots-prospect/
!/.claude/skills/boots-track/
!/.claude/skills/boots-surface/
!/.claude/skills/boots-rethink/
!/.claude/skills/boots-extract/
!/.claude/skills/boots-retire/
!/.claude/skills/boots-observe/
!/.claude/skills/boots-upgrade/

# Local-clone runtime state — created when Boots runs in this dev repo in place.
# The real per-user copies live under ~/.boots/; these top-level artifacts are
# NOT part of the product and must never be committed. systems/ in particular
# holds the author's own private systems (the same data ~/.boots/systems/ holds
# for a user). CLAUDE.md and IDEAS.md are deliberately absent here — both ARE
# tracked contributor docs.
/.activated
/.consent-prompted
/.last-setup-version
/analytics/
/config.yaml
/installation-id
/just-upgraded-from
/last-update-check
/sessions/
/systems/
/update-snoozed

# Supabase CLI local state (version cache, temp files) — created by the CLI, never
# part of the product. The committed backend is supabase/{migrations,functions,config.sh}.
/supabase/.temp/

# Eval corpus — RAW session transcripts contain real client PII (names, account
# numbers, balances from live sessions). Never commit. The distilled, redacted
# eval CASES under evals/cases/ are safe and ARE committed.
/evals/transcripts/
state/systems/reply-guard/.seen.json
